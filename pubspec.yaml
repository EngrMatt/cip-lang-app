name: cip_lang_app
description: 田野語料收集 Flutter App（iOS + Android 純前端）
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^14.8.1
  dio: ^5.8.0+1
  record: ^6.0.0
  just_audio: ^0.9.46
  image_picker: ^1.1.2
  path_provider: ^2.1.5
  intl: ^0.20.2
  permission_handler: ^11.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
