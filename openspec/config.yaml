schema: spec-driven

context: |
  Project: cip-lang-app — Flutter 純前端田野語料收集 App（iOS + Android）
  Spec source: docs/mvp.md | API spec: docs/api.md | Design: design/ (Field Ethos)
  Architecture: pure frontend client; all data via REST API to external backend
  API base URL (default): https://cip-lang-test-20260624.nfs.tw (Swagger: /docs)
  Local override: --dart-define=API_BASE_URL=http://10.0.2.2:8280

  Tech stack: Flutter 3.x, Dart, Riverpod, Dio, go_router, record, just_audio,
    image_picker, shared_preferences, audio_waveforms, hive, image_cropper
  MVP + Phase 1.5: record list (filters), surveyor checkin, create wizard (drafts,
    indigenous symbols), audio waveform, photo crop, upload
  Design system: Field Ethos — primary #001813, surface #fbf9f8, Hanken Grotesk style
  Out of scope: auth, GPS, offline background sync, AI, backend code in this repo

  Conventions: feature-first lib/, Riverpod, repository layer for API, 繁體中文 UI

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
